---
intro: "The `incl/main` folder is for `.tex` files with the main contents of the document."
file:
  before: "Each file should begin with"
  after: "and should only contain a single chapter."
  name: "Chapter name"
  label: "labelname"
mainmatter:
  - line: "Chapter files are included in the document with the `\\include` command _after_ the `\\mainmatter` command:"
---
